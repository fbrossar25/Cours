#include<stdio.h>
#include<string.h>

int main (){
	char prenom[40], nom[40];
	printf("SVP prenom ? ");
	scanf("%s",&prenom);
	printf("SVP nom ? ");
	scanf("%s",&nom);
	while(prenom[i]!=0)
	return 0;
	
}
