import { StatePrestaDirective } from './state-presta.directive';

describe('StatePrestaDirective', () => {
  it('should create an instance', () => {
    const directive = new StatePrestaDirective();
    expect(directive).toBeTruthy();
  });
});
